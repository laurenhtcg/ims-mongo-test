"""Seed orders (populate job — implement with inserts or merge pipelines as needed)."""


def seed_orders() -> None:
    """Future: long-running job to build or refresh order-related collections (e.g. ``$merge`` from staging)."""

