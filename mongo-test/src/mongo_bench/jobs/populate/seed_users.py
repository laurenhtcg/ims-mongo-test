"""Seed users (populate job — implement with inserts or merge pipelines as needed)."""


def seed_users() -> None:
    """Future: long-running job to build or refresh user-related collections (e.g. ``$merge`` from staging)."""

